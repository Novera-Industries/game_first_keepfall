# ── Unity ───────────────────────────────────────────────
unity/[Ll]ibrary/
unity/[Tt]emp/
unity/[Oo]bj/
unity/[Bb]uild/
unity/[Bb]uilds/
unity/[Ll]ogs/
unity/[Uu]ser[Ss]ettings/
unity/[Mm]emoryCaptures/
unity/.vs/
unity/*.csproj
unity/*.sln
unity/*.unityproj
*.apk
*.ipa
*.app

# ── Node / Cloudflare Workers ───────────────────────────
node_modules/
backend/.wrangler/
backend/dist/
backend/.dev.vars
*.log
.DS_Store

# ── Secrets ─────────────────────────────────────────────
.env
.env.local
*.secret
GoogleService-Info.plist
firebase-adminsdk*.json

# ── Editor ──────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json

# ── Accidental nested clone of this repo ────────────────
# A second clone of this repository was created inside the working tree
# (game_first_keepfall/). It must never be tracked by the outer repo.
/game_first_keepfall/
